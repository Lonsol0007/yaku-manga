# Build-time only. Neither belongs in the repo: the venv is ~3 GB of PyTorch, and
# out/ holds the exported weights, which are hundreds of megabytes per pack.
.venv/
out/
__pycache__/
# The onnxruntime version the app ships, installed on first use to check packs against.
.android-ort/
